name: Stringer CI/CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build_tests_publish:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --configuration Release --no-restore --verbosity minimal
    - name: Test
      run: dotnet test --no-restore --verbosity minimal
    - name: Publish NuGet
      uses: brandedoutcast/publish-nuget@v2.5.5
      with:        
        PROJECT_FILE_PATH: "./Stringer/Stringer.csproj"
        NUGET_KEY: ${{ secrets.NUGET_API_KEY }}         
    #- name: Publish
    #  run: dotnet pack --configuration Release --no-restore --verbosity minimal ./Stringer/Stringer.csproj
      
